# Copy to .env for local dev (.env is gitignored — never commit a real key).
# On a deploy platform, set this as a secret/environment variable instead.
OPENAI_API_KEY=sk-your-key-here

# Optional overrides:
# IMAGE_MODEL=gpt-image-2
# PORT=3000
